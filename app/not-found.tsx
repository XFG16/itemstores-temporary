import PublicLayout from "./(public)/layout";
import PublicNotFoundPage from "./(public)/not-found";

export default function NotFoundPage() {
  return (
    <PublicLayout>
      <PublicNotFoundPage />
    </PublicLayout>
  );
}
